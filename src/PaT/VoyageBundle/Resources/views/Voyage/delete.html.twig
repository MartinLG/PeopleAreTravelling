{% extends "PaTVoyageBundle::layout.html.twig" %}

{% block title %}
  Supprimer un Voyage - {{ parent() }}
{% endblock %}

{% block Column_centrale %}
  <h2>Supprimer un article</h2>

  <br>
  
  <p>
    Etes-vous certain de vouloir supprimer le voyage "{{ voyage.title }}" ?
  </p>

  <form action="{{ path('pa_t_voyage_delete', {'id': voyage.id}) }}" method="post">
    <a href="{{ path('pa_t_voyage_homepage', {'id': voyage.id}) }}" class="btn">
      <i class="icon-chevron-left"></i>
      Retour à l'article
    </a>
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {{ form_rest(form) }}
  </form>
{% endblock %}