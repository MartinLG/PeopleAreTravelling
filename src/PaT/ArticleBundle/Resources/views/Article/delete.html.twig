{% extends "PaTArticleBundle::layout.html.twig" %}

{% block title %}
  Supprimer un Article - {{ parent() }}
{% endblock %}

{% block Column_centrale %}
  <h2>Supprimer un article</h2>

  <br>
  
  <p>
    Etes-vous certain de vouloir supprimer l'article "{{ article.title }}" ?
  </p>

  <form action="{{ path('pa_t_article_delete', {'travelid' :  travelid , 'id': article.id}) }}" method="post">
    <a href="{{ path('pa_t_voyage_homepage', {'id': article.id}) }}" class="btn">
      <i class="icon-chevron-left"></i>
      Retour à l'article
    </a>
    <input type="submit" value="Supprimer" class="btn btn-danger" />
    {{ form_rest(form) }}
  </form>
{% endblock %}